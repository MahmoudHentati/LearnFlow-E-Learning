// Chart helper utilities
console.log('chart-helper.js loaded');

window.chartHelper = {
    createChart: function(canvasId, config) {
        console.log('Creating chart for:', canvasId);
        // Chart creation logic
        return {};
    },
    updateChart: function(chartId, data) {
        console.log('Updating chart:', chartId);
        // Chart update logic
    },
    destroyChart: function(chartId) {
        console.log('Destroying chart:', chartId);
    }
};
